import Image from "next/image";

## Notify Editor

The `notify-editor` command grants access to the customization interface, available only to administrators with the permission added to the `server.cfg`, as set by the previous command.

---

<Image
  src="/notify-interface-demo-beta.png"
  alt="Hello"
  width={1920}
  height={1080}
  className="rounded"
/>
