import Image from 'next/image'
 
## Notify Editor
 
O comando `notify-editor` faz que você tenha acesso a interface de personalização, apenas para administradores com a permissão inserida no `server.cfg`, concebida pelo comando anterior.

---

<Image
  src="/notify-interface-demo-beta.png"
  alt="Hello"
  width={1920}
  height={1080}
  className="rounded"
/>
